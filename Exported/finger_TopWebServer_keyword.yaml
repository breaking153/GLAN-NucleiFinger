id: TopWebServer_keyword
info:
  name: TopWebServer_keyword
  author: GLAN
  severity: info
  tags: tech
http:
  - method: GET
    path:
      - "{{RootURL}}{{Path}}/"
    cookie-reuse: true
    matchers-condition: or
    redirects: true
    matchers:
      - type: word
        part: header
        name: "TopWebServer"
        words:
          - "TopWebServer"
        condition: and