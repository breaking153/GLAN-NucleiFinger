id: kubernetes_keyword_1
info:
  name: kubernetes_keyword
  author: GLAN
  severity: info
  tags: tech
http:
  - method: GET
    path:
      - "{{RootURL}}{{Path}}/"
    cookie-reuse: true
    matchers-condition: or
    redirects: true
    matchers:
      - type: word
        part: header
        name: "kubernetes"
        words:
          - "realm=\"kubernetes"
        condition: and