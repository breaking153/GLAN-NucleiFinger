id: Tableau_Server_keyword
info:
  name: Tableau Server_keyword
  author: GLAN
  severity: info
  tags: tech
http:
  - method: GET
    path:
      - "{{RootURL}}{{Path}}/"
    cookie-reuse: true
    matchers-condition: or
    redirects: true
    matchers:
      - type: word
        part: header
        name: "Tableau Server"
        words:
          - "Tableau Server"
        condition: and