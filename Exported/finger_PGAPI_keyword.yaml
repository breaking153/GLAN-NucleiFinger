id: PGAPI_keyword
info:
  name: PGAPI_keyword
  author: GLAN
  severity: info
  tags: tech
http:
  - method: GET
    path:
      - "{{RootURL}}{{Path}}/"
    cookie-reuse: true
    matchers-condition: or
    redirects: true
    matchers:
      - type: word
        part: header
        name: "PGAPI"
        words:
          - "PGAPI"
        condition: and